import React from 'react'

export function Banner() {
  return (
    <div className="banner">
      <div className="container banner-inner">
        <p className="tagline">Find your next role or post your next hire — fast.</p>
        <p className="hint">Tip: Use the header search, filter by type, and sort by date.</p>
      </div>
    </div>
  )
}
